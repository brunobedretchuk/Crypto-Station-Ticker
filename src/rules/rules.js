import {defineRule} from 'vee-validate';
// examples of rules usable with vee-validate

// defineRule('positive', value => {
//     if (value > 0){ return true}
//     else{ return 'Number must be positive!'}
//   });

//   defineRule('matchPasswords' , (value , [target]) => {
//     if (value === target) {
//         return true;
//       }
//       return "Passwords don't match";
//   })

//   defineRule('isRequired' , value => {
//     if (value && value.trim()) {
//         return true;
//       }
//       return "Obligatory input";
//   })
  
//   defineRule('passwordLength' , value => {
//     if (value.length >= 8) {
//         return true;
//       }
//       return 'Password must contain at least 8 characters'
//   })
